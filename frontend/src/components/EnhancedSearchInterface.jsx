import React from 'react';
import ChatInterface from './ChatInterface';

const EnhancedSearchInterface = ({ docType, onDocTypeChange }) => {
  // For now, just render the ChatInterface which is the actual working component
  return <ChatInterface />;
};

export default EnhancedSearchInterface;